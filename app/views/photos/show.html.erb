<%= render partial: "photo", locals: { photo: @photo }, as: :photo %>

<%= render partial: "comments/comment", collection: @comments, as: :comment %>

<%= render partial: "photo_sidebar", locals: { album: @album, photo: @photo } %>